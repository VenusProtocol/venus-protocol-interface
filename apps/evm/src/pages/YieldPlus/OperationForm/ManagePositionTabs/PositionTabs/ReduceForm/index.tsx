import type { YieldPlusPosition } from 'types';

export interface ReduceFormProps {
  position: YieldPlusPosition;
}

export const ReduceForm: React.FC<ReduceFormProps> = () => {
  return 'TODO: render Reduce form here';
};
