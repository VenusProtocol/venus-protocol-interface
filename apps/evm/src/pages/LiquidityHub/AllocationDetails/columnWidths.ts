// Shared by the yield group table and the source tables nested inside it, so the same metric
// always sits at the same horizontal position
export const COLUMN_WIDTHS = ['26%', '14%', '14%', '12%', '16%', '18%'];
