{
  "name": "@venusprotocol/web3",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "engines": {
    "node": ">=18.x.x"
  },
  "scripts": {
    "generate-contracts": "src/scripts/generateContracts/index.ts",
    "generate-pancake-swap-tokens": "src/scripts/generatePancakeSwapTokenList/index.ts",
    "generate": "rimraf src/generated && npm-run-all --parallel generate-contracts generate-pancake-swap-tokens && yarn prettier --write src/generated/**/*.ts",
    "tsc": "tsc --noEmit",
    "test": "vitest run"
  },
  "publishConfig": {
    "access": "public"
  },
  "peerDependencies": {
    "ethers": "5.x",
    "react": "18.x"
  },
  "devDependencies": {
    "@ethersproject/abstract-provider": "^5.7.0",
    "@typechain/ethers-v5": "^11.1.1",
    "handlebars": "^4.7.8",
    "node-fetch": "^3.3.2",
    "npm-run-all2": "^6.0.6",
    "path": "^0.12.7",
    "prettier": "^3.0.3",
    "typechain": "^8.3.1",
    "vitest": "^0.34.4"
  }
}
